# Backend API base URL. Leave empty for relative URLs (recommended) — the
# FastAPI backend serves the Astro build and the API on the same origin,
# so /api/chat etc. resolve to the same host the page was loaded from.
# Override only when you want the storefront to talk to a remote backend.
PUBLIC_API_BASE=
